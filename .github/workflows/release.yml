name: Release Elyxion CLI

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22.x'

    - name: Install build dependencies (npm)
      run: npm install --ignore-scripts

    - name: Build native addon
      run: npx node-gyp rebuild

    - name: Run tests
      run: node test/basic.test.js

    - name: Package for Linux
      if: matrix.os == 'ubuntu-latest'
      run: |
        tar -czf elyxion-linux-x64.tar.gz \
          -C . \
          bin/ \
          lib/ \
          build/ \
          package.json \
          package-lock.json \
          README.md \
          LICENSE

    - name: Package for macOS
      if: matrix.os == 'macos-latest'
      run: |
        tar -czf elyxion-macos-x64.tar.gz \
          -C . \
          bin/ \
          lib/ \
          build/ \
          package.json \
          package-lock.json \
          README.md \
          LICENSE

    - name: Package for Windows
      if: matrix.os == 'windows-latest'
      run: |
        7z a elyxion-windows-x64.zip bin/ lib/ build/ package.json package-lock.json README.md LICENSE

    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: release-${{ matrix.os }}
        path: |
          elyxion-*.tar.gz
          elyxion-*.zip

  create-release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Download all artifacts
      uses: actions/download-artifact@v4
      with:
        path: artifacts/

    - name: Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        generate_release_notes: true
        files: |
          artifacts/release-ubuntu-latest/*
          artifacts/release-macos-latest/*
          artifacts/release-windows-latest/*
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  publish-elyx-registry:
    needs: create-release
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22.x'

    - name: Install build dependencies (npm)
      run: npm install --ignore-scripts

    - name: Build
      run: npx node-gyp rebuild

    - name: Publish to Elyxion Registry
      run: node bin/elyx publish
      env:
        GITHUB_TOKEN: ${{ secrets.ELYX_REGISTRY_TOKEN }}
