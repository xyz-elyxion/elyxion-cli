# Local standalone V8 SDK (headers + v8_monolith)
.v8/

# CMake build output
build/

# Registry server runtime state (users, tokens, packages)
site/data/

# Built site output (regenerated by site/build.js at boot if missing)
site/dist/
